import "./Loader.css";

/**
 * Loader component that renders a loading indicator.
 * 
 * @component
 * @returns {JSX.Element} A div element with a class of "loader" to indicate loading state.
 */
function Loader() {
  return <div className="loader"></div>;
}

export default Loader;
